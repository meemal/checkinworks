export interface NavigationItem {
  label: string;
  path: string;
  external?: boolean;
}

export interface ImpactStat {
  number: string;
  description: string;
}

export interface FeatureCard {
  title: string;
  description: string;
  icon: string;
}
